// écritures des variables

function effacerTxtInput(input) {
    if (input.value === "tapez votre nom ici" || input.value === "monemail@domaine.com" || input.value === "tapez votre message ici") {
        input.value = ""
    }}